# Managed by pi-server repo — deployed via install.sh
# Auto-restart services after library updates (no interactive prompts)

$nrconf{restart} = 'a';

# Exclude system-critical services from auto-restart
$nrconf{override_rc} = {
    qr(^dbus) => 0,
    qr(^systemd-logind) => 0,
};
